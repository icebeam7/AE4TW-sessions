# Step 1: Prerequisites

1. Install Node.js (LTS).

- Reference: Node.js downloads: https://nodejs.org/

1. Confirm you have the `node` and `npm` tools (for example, by checking the version)

1. Create a new folder `node-express-api` and set it as the current working directory.

[< Previous Step](overview.md) | Step 1 | [Next step >](step2.md)